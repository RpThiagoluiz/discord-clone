//Components
import ServerList from "../ServerList";
//Styles
import { Grid } from "./styles";

const Layout = () => {
  return (
    <Grid>
      <ServerList />
    </Grid>
  );
};

export default Layout;
